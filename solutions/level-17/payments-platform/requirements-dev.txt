# The tools that make the service correct before it ships, and must never be in
# the image that ships. Measured cost of getting that wrong: 88.6 MB and 3,277
# files, none of which executes in production.
-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
hypothesis==6.112.1
mypy==1.11.2
ruff==0.6.8
